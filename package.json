{
  "name": "prisma-press-first",
  "module": "index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "stripe:webhook": "stripe listen --forward-to localhost:5000/api/subscription/webhook",
    "start": "bun src/app.ts"
  },
  "dependencies": {
    "stripe": "^22.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
